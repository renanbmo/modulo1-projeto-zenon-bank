services:
  db.transactions:
    image: mysql:latest
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: senha123
      MYSQL_DATABASE: transactions
    ports:
      - 3306:3306
    volumes:
      - mysql-transactions:/var/lib/mysql
volumes:
  mysql-transactions: